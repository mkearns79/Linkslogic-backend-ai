# Production Hybrid System Requirements
# Deployment-optimized: ~50MB total vs 500MB+ with local ML

flask==3.0.0
flask-cors==4.0.0
openai==1.54.3
python-dotenv==1.0.0

# Lightweight math for cosine similarity
# (No need for heavy numpy since we only need basic math operations)

# REMOVED heavy dependencies that caused deployment issues:
# ❌ sentence-transformers==2.2.2  (300MB+ package)
# ❌ torch==2.0.1+cpu             (500MB+ package)  
# ❌ scikit-learn==1.3.2          (100MB+ package)
# ❌ transformers==4.30.2         (200MB+ package)

# All sophisticated AI functionality preserved via OpenAI APIs
